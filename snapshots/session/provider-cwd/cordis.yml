# The filesystem fixture supplies execution coordinates without a remote service.
- id: fs-sandbox
  disabled: true

- insert:
    - id: provider-cwd-fs
      name: '../../../packages/bundle/headless/tests/fixtures/provider-cwd.ts'
      config:
        root: !!js process.env.DSH_SNAPSHOT_PROVIDER_CWD
